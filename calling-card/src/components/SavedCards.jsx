import { useParams } from "react-router-dom";

const Saved = () => {
    const { saved }=useParams();
    return (
        <div>
            <p>Placeholder</p>
        </div>
    )
}
export default Saved